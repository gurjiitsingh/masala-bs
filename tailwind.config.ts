/** @type {import('tailwindcss').Config} */
const config = {
    darkMode: false, // Disable dark mode
    content: [
      "./src/**/*.{js,ts,jsx,tsx}", // Adjust this path to fit your project structure
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  
  export default config;